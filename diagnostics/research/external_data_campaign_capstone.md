# External-Data Campaign — Capstone Retrospective (2026-07-28; input to the Oct-1 review)

**Final state: screens 11 · sealed opens 1 · n_trials 138 — the entire campaign spent ZERO trials.**
The campaign is PAUSED as pre-declared; the next session is the Oct-1 governance package.

## What was acquired and banked (all PIT-proven, all committed, all reusable)
| dataset | state |
|---|---|
| **Delivery %** (1,949 days, 3.49M rows, 2019→) | BANKED. Audit 4/4 (byte-identical format seam, 813 delisted symbols). Screen PASS. Usage priced out. |
| **Earnings calendar** (58,871 results-events, two-layer PIT, 2019→) | BANKED. Audit passed (lag median 8d; SEBI's ≥5d rule visible). Screen PASS. Usage priced out. S5 resolved-positive at screen level. |
| Bulk/block deals | Acquisition-blocked (API bot-wall; no archive route). Harvester committed; forward-only fallback standing. |
| Credit ratings (16k filings 2023→) | Raw banked but **no equity linkage** (free-text symbol field; debt-instrument stream) — coverage-KILL. |

## The scoreboard
- **Two screen PASSES** — real, orthogonal-to-price information exists and was found exactly where
  0117 said it would have to live: dlv_med21 (+0.363R conditional, ADV-robust) and known-event
  proximity (−0.383R, 5/6 years). The screens work; the information is real.
- **Three activation bounds, 3/3 kills** — each with a mechanism, each pre-trial, each ~free:
  rotation (~11R/yr requires clairvoyance), delivery tiebreak (−1.29R/yr: the gradient dies at the
  CRS-adjacent margin), earnings deferral (−15.7R/yr: 94% lapse — the setup does not survive the
  event, and worse-than-average is still positive-EV).
- **One coverage-KILL** (ratings) and **three paper-precondition closures** (#5-#7) — priced without
  harvests.

## The law the campaign generalized (now enforced, 3/3 precedents)
**Population/cohort information is real; this book's decision margins — selection, timing, exit —
cannot express it; worse-than-average is still positive-EV.** Corollaries now standing: the
activation-bound gate is mandatory before any usage trial; label screens are serialized and priced in
the ledger; sealed opens are priced like any reuse.

## What would have to change for the banked assets to find a consumer
A structurally different book shape — one whose decision points CAN express population-level quality
gradients: lower-turnover selection with slack capital (no week-relative contention), or
portfolio-level weighting across many simultaneous positions (the 0081/0107 multi-sleeve direction),
or a longer-horizon sleeve where a 0.3R population edge compounds instead of dying at a marginal slot.
That is forward-wall / owner territory (0115: a third leg needs a different return source; 0081: the
blend is the structure of record). The datasets, PIT layers, harvesters, and the label verdict machine
all carry over unchanged whenever that question is opened.

## Ledger cross-reference
`label_screen_ledger.md` rows 1-11 (all closed), sealed-opens S1, the activation-bound law section.
Findings: 0116-0122. The 0094 golden was byte-identical throughout; engine untouched by the campaign.
