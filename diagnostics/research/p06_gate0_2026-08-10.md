# P0.6 — Gate 0 on the two queued screens, and both plan premises fail

**MEASUREMENT, no trial. No screen-ledger row: nothing here touched the banked label dataset.**
Standing counts: **screens 19 · sealed opens 1 · n_trials 2** (authorities:
`diagnostics/research/label_screen_ledger.md` line 38, `diagnostics/research/n_trials.json`).

The 2026-08-09 plan queued two "zero-trial screens likely to close for free". Gate 0 says neither is
worth running, and the plan's framing was wrong on more counts than the candidates were.

## The framing error first

The plan said both candidates were "likely to close for free". **Under the owner amendment of
2026-08-08 no screen or bound can close anything.** Gate 3 was explicitly amended: it "no longer
kills anything… a ceiling below the ±10R/yr floor is a *bound*, not a verdict". A negative verdict
now requires a pre-registered run under the current harness. So the entire premise — cheap closure —
does not exist any more. A bound buys **priority**, not closure.

The plan also stated Gate 3 is "5/5 and has never passed". It is **3/3** (0117 rotation, 0119
tiebreak, 0121 deferral), per `skills/verdict-machine`.

## Candidate A — BREADTH_REG activation bound: NOT WORTH RUNNING

| idea element | verdict | open/closed | citation |
|---|---|---|---|
| continuous breadth + index-trend exposure scalar | **already run as a full trial** | KILL (2026-06, momentum book) | `preregistry/0070-crash-overlay.md:55-68` |
| regime / market-state gating as a class | closed as a class | CLOSED | program-laws IX (O-001, 0056, 0086, 0090, 0103) |

0070 is `Status — COMPLETE`, `VERDICT — formal KILL (no single-signal arm clears all SHAPE gates)`.
BREADTH_REG was its *best* arm — best Calmar 0.668 vs 0.623 — and still failed the shape gates.

An activation bound measures a **clairvoyant ceiling** for a rule that has never been run. 0070 ran
this one for real and measured its actual performance. A hindsight ceiling on an already-measured
rule adds nothing, and under the amendment it could not close it even if it were damning.

What the amendment *does* say is that 0070's KILL is old-epoch: 2026-06-26, before the
continuous-slice fix, the calendar-annualisation fix and the v0→v1 re-anchor, and measured on the
**momentum** book rather than the live swing book. So it is quotable as
`KILL (2026-06 epoch, momentum book) — not re-tested under the current harness`, and it sets
priority. Re-opening it costs a trial. Against n_trials 2 and a live book that is already
UNCERTIFIED at DSR 0.8096, that trial should not be spent here.

## Candidate B — Clenow ρ-screen: the "defects" are real and immaterial

`clenow_score` exists at `nq/signals/__init__.py:132` with four tests and **no registry row** — the
"code present ≠ study run" trap that produced three false conclusions on 2026-08-09. It has never
been studied. Before proposing anything, the plan's three claimed defects were measured:

| claim | measured | verdict |
|---|---|---|
| `np.exp(slope) ** periods` overflows above slope ≈ 2.8 | overflows at **2.839** — correct | **immaterial**: slope is a per-day log return, so 2.839 means **17× per day** |
| the two-step power loses precision vs `exp(slope*periods)` | relative difference **1.1e-14 … 2.7e-14** | immaterial |
| `periods=250` vs `YEAR_DAYS=252` "genuinely re-orders" | **2,058 of 4,000 ranks move** — technically true | **immaterial for this book**: Spearman **0.999999**, max shift **7 places**, and **top-30 membership changes: 0** |

The momentum book takes `top_n=30`. A defect that cannot alter the top-30 set cannot alter a single
holding. All three are cosmetic on real inputs. The plan called them defects to fix "before anything
is pinned"; the honest status is a tidy-up with no decision content.

The remaining ρ-screen idea — measure rank correlation between Clenow and NMS, and deprioritise the
head-to-head if they agree — is still *legal* (it reads the price panel, not the banked labels, so it
costs no ledger row). It is simply low-value: Clenow ranks the **momentum** book, and only 7 of 108
closed verdicts bind the live swing book. Not run, not closed, ranked last.

## What this actually tells us

Both queued candidates evaporated on contact with the registry, and neither needed a measurement to
do it. That is the verdict machine working as designed — Gate 0 is free and most ideas die there.

It also says the cheap-screen well is dry, which is consistent with the external-data campaign being
capstoned at zero trials. There is no free research win sitting in the queue. The programme's
bottleneck is not unexamined ideas; it is **evidence**, and evidence now accrues only on the forward
calendar or on a genuinely new sample.

Reproduce the Clenow measurements: the three-part check in the commit message for this file, or
re-run the arithmetic against `nq/signals/__init__.py:132-163`.
