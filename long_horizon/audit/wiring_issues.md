<!-- Provenance: Phase-1 wiring audit, 2026-06-27.
Method: 9 parallel auditors each ran python probes against the REAL data files
(results/ohlcv_cache_lh.json, data/nifty500_membership.csv, data/fundamentals_pit_screener.pkl)
+ read the live code; every red/HIGH finding was adversarially re-verified by a second agent;
the two top red wires (W-01 VEDL demerger-as-split, W-02 AUD-007 universe gap) were then
independently re-confirmed by the lead with exact numbers (VEDL slope 2.156->24.944,
splits_adjusted=1; current-members-invisible = 48 < 75 alert threshold).
This is a LIVE-WIRING + DATA-INTEGRITY audit; it does NOT re-validate the headline backtest
(see the reproducibility verdict -- that needs a cloud cpcv-research.yml run). -->

# Long-Horizon Strategy — Wiring Issue Ledger

Sorted CRITICAL → LOW. "Blocks P2" = must be fixed before a new research layer / cloud backtest can be trusted.

| id | sev | wire | title | file:line | evidence | fix | conf | blocks P2? |
|---|---|---|---|---|---|---|---|---|
| W-01 | **HIGH** | 🔴 red | Demerger back-adjusted as split → fabricated top-3 BUY (VEDL) | `data_store.py:118`; `ohlc_panel.py:52`; `long_horizon_cron.py:295` | `_CORP_ACTION_MOVE=0.50` scales pre-event bars; VEDL splits_adjusted=1, slope **+2.16→+24.94**, rank **3/495**, only adjusted name in top-20 (re-confirmed this session). Blocked from book today only by coincidental D/E=2.22 solvency fail; becomes a live false BUY when D/E rolls to 0.56 (avail 2026-06-29) | Source real CA type from `yf.Ticker.actions`; only back-adjust confirmed splits. For unconfirmed >50% drops, regime-split (NaN the slope until 263 clean post-event bars) instead of multiplying. Min-viable: quarantine names with `splits_adjusted>0` within the last 263 bars from the top-K. Add a VEDL regression test | high | **YES** |
| W-02 | **HIGH** | 🔴 red | AUD-007: 48 current index members invisible to the scan | `long_horizon_cron.py:285-294`; `index_membership.py:140` | config.NIFTY_500 (2025-07-20) & current = 452; current−config = 48 unrankable (ATHERENERG/GROWW/MEESHO — newest high-momentum, exactly what slope targets); gate silent (alert=75 > 48) | Set `ds.stocks = sorted(current_members(membership, today) ∪ set(config.NIFTY_500))` before download (~line 242); PIT mask at :284 drops non-members safely. Lower `MEMBERSHIP_DIVERGENCE_ALERT` to ~30 or add an "invisible current members" degrade | high | **YES** |
| W-03 | **HIGH** | 🔴 red | All cron alerts are silent no-ops | `alert_publisher.py:185`; `cron-scanner.yml:44` | `get_configured_publisher()` returns None unless `DRIFT_ALERT_*` set; `grep -rn DRIFT_ALERT .github/workflows/` → nothing. A degraded-but-exit-0 run or a dropped scheduled run = zero notification; hard crashes still caught by GH's own failure email | Add `DRIFT_ALERT_SLACK_WEBHOOK: ${{ secrets.… }}` to cron-scanner.yml + cron-sector-regime.yml env (activates every existing `_alert()`). Add a missed-run watchdog (external ping / scheduled Action) since a never-started run writes no cron_health | high | no (ops, not data-trust) |
| W-04 | **HIGH** | 🟠 amber | Financials sector silently excluded by D/E filter | `long_horizon_cron.py:359`; `fundamentals_pit.py:433`; `sheets.py:58` | 78-79 large+mid dropped; 62 = banks(26)+NBFC(36) with D/E=NaN (Screener Borrowings/net-worth ill-defined for deposit-takers); HDFCBANK/ICICIBANK/SBIN/BAJFINANCE all dropped. live==backtest (solvent_low_debt=397/682) so NOT a parity bug | Decide explicitly: (a) special-case Banking/NBFC/Insurance to skip D/E (or use a capital-adequacy proxy), OR (b) keep the drop but make it LOUD — `diag['solvency_dropped_financials']` + cron_health stat + amend STRATEGY.md "financials excluded". Distinguish "D/E NaN/absent" from "D/E≥1.5" | high | partial (narrows universe vs spec; doc + observability before any breadth/sector-neutral claim) |
| W-05 | **HIGH** | 🟠 amber | Live records stop exits AT the stop (optimistic vs backtest gap-fill) | `signal_tracker.py:467,510`; `exit_logic.py:256`; `portfolio.py:323,327` | Live passes no `open` → gap branch can't fire, then hardcodes `close_price=stop`. 848/20k fuzz bars diverge; 297/1453 stop exits (20.4%, avg −17%, 199 worse than −15%) are gap-throughs. Surfaces in signals_history.json → WR/kill-criteria (rollback-trigger source) | (1) pass `today_ohlc={'open':current_open,'close':price,'high':…,'low':…}` (current_open at :375). (2) book `sig['close_price']=decision.exit_price` and recompute pnl from it | high | **YES** (paper track record is the real-capital gate; optimism on its worst 20% defeats it) |
| W-06 | **MEDIUM** | 🔴 red | kill_log.jsonl audit trail never pushed/tracked | `long_horizon_cron.py:577,581,667`; `github_push.py:63` | Written via `log_verdict` but omitted from explicit push list (only kill_state.json at :581); untracked (`git ls-files` empty). Discarded each ephemeral run. (Severity corrected HIGH→MEDIUM: kill_state.json IS pushed, so the gate STATE machine is intact; only the human triggered-verdict audit log is lost, and it only writes on a triggered metric) | Append `'results/kill_log.jsonl'` to paper_files at :581 (push_results skips non-existent files → safe). Optionally seed an empty committed file | high | no (gate functions; observability only) |
| W-07 | **MEDIUM** | 🟠 amber | Headline not reproducible locally; equity curve not persisted | `run_long_horizon_tradelog.py:48,71`; result JSONs | data/ohlcv.pkl + data/features.pkl ABSENT (confirmed); committed JSONs have no `equity_curve` (all 4 → False). Survivor-only live cache ≠ backtest dataset. cpcv-research.yml DOES rehydrate ~247 dropped names, but ~114 hard bankruptcies (DHFL/RCAP/SREINFRA) unrecoverable from yfinance | Persist `res['equity_curve']` into the committed JSON/CSV (one line, :71). Document the rehydration command + hash in long_horizon/results/README.md. Add a panel-coverage assert: error if any membership name in [2019,2026) has zero OHLCV in its window. Source the 114 from Kite dumps | high | **YES** (can't trust/re-derive a number before building on it) |
| W-08 | **MEDIUM** | 🟠 amber | Cache outside .gitignore allowlist → latent 600d cold-start | `.gitignore:23-41`; `ohlcv_incremental.py:426` | `results/*` + allowlist; `_lh` cache NOT whitelisted. Persists only because tracked; `git rm --cached`/clean → `results/*` hides it → cold-start every run (495-name yfinance hammer) | Add `!results/ohlcv_cache_lh.json` after the `!results/ohlcv_cache.json` line | high | no |
| W-09 | **MEDIUM** | 🟠 amber | pyarrow installed unhashed outside the lock (SPOF) | `cron-scanner.yml:36-42` | Separate `pip install pyarrow` outside `--require-hashes`; without it load_fund_store fails → all D/E NaN → empty universe. PyPI blip = outage; also same gap in cpcv-research.yml | Add pyarrow (pinned+hashed) to requirements.lock and drop the separate step; else pin `pyarrow==<ver>` in both workflows | high | partial (reproducibility of the env) |
| W-10 | **MEDIUM** | 🟠 amber | Names with zero fundamentals silently dropped (no coverage gate) | `long_horizon_cron.py:351-359`; `sheets.py:37` | 27-28 large+mid absent from store (ABBOTINDIA/HONAUT/PAGEIND…) → all-NaN low_debt → dropped; only freshness gated, never coverage; a future scrape regression shrinks the universe invisibly until <50 | After solvency join compute `diag['solvency_no_coverage']` and `solvency_coverage = covered/eligible`; degrade in assess_data_health if coverage < ~0.55. Separate "absent from store" vs "present but D/E NaN" | high | partial |
| W-11 | **MEDIUM** | 🟠 amber | Live ages +1 trading day vs backtest | `signal_tracker.py:399`; `portfolio.py:299` | Live counts days_since from signal_date(t); backtest from fill day(t+1). 63d cap & 10d min-hold fire one session early live (verified: next-session age=1 live vs 0 backtest) | Stamp assumed fill date (signal_date + 1 trading day) and age from it, or `days_held_for_exit = max(0, days_since-1)`. Add a same-age regression | high | no |
| W-12 | **MEDIUM** | 🟠 amber | Kill-equity curve books entry at close(t), not open(t+1) | `signal_tracker.py:379-390`; `paper_equity.py:79-92` | Always-on equity (feeds kill gate via portfolio_history.csv) uses close-based entry/stop/target → mildly optimistic + inconsistent with paper_broker ledger (open-based) | On first track after signal_date set tracked entry = current_open·(1+slip) and re-anchor stop/target; or document the curve is ~p90 1.4% optimistic on the entry leg | high | no (subsumes with W-05 fix) |
| W-13 | **MEDIUM** | 🟠 amber | fund-store relative path → silent empty universe if cwd≠root | `build_measurement_panel.py:26`; `long_horizon_cron.py:341` | `'data/fundamentals_pit_screener.pkl'` cwd-relative; non-root invocation → store=None → all NaN → empty universe (masked as generic "empty universe"). Works via `python -m` from root | `FUND_STORE_PATH = Path(__file__).resolve().parents[2]/'data'/'fundamentals_pit_screener.pkl'`; distinguish FileNotFoundError from corrupt-pickle | high | no |
| W-14 | **MEDIUM** | 🟠 amber | Silent per-name `except: pass` can drop a whole name | `data_store.py:254-260` | A crash in any of ~44 unused columns silently discards the name (incl. good slope), no log. 0/495 today — latent | Replace with `except Exception as e: dropped.append((ticker, repr(e)))` + summary count; ideally a `mode='live'`/`compute_labels=False` path that skips the ~44 unused columns | high | no |
| W-15 | **LOW** | 🟠 amber | GitHub push returns True on PARTIAL success → no alert | `github_push.py:188`; `long_horizon_cron.py:672` | Per-file REST commits; returns `success>0`; cron alerts only if ALL fail. A dropped signals_today.json (pushed last) leaves dashboard inconsistent for ~24h, no alert | Return `len(failed)==0` (or the failed list); alert on ANY failure: `if failed: _alert('degraded', f'push partial {failed}')` | high | no |
| W-16 | **LOW** | 🟠 amber | Kite refresh cron missing; runbook points at non-existent file | `MIGRATION_RENDER_TO_SUPABASE.md:14`; `refresh_kite_session.py` | `cron-kite-refresh.yml` does not exist (`ls` → not found). Dashboard Kite session expires 6 AM IST with no scheduled refresh. ZERO impact on signal scan (Kite not on price path — confirmed by grep) | Port refresh_kite_session.py to a `cron-kite-refresh.yml` (`45 0 * * 1-5`) with DB/Zerodha secrets, OR correct the runbook if refresh runs on Fly/Supabase | high | no |
| W-17 | **LOW** | 🟠 amber | sub-50% demerger left as raw discontinuity | `data_store.py:118`; `dv_ohlcv_integrity.py:42` | TRENT −33% / IEX −30% / INDUSINDBK −27% splits_adjusted=0; benign now (negative slopes pushed out); latent future false-positive; DOWN_JUMP=−0.45 doesn't even record them | Band-detect 20-65% persistent non-reversing drops + corroborate via yf actions; lower DOWN_JUMP to ~−0.25 for monitoring; surface jump count in cron_health | high | no |
| W-18 | **LOW** | 🟠 amber | 0-share BUY signal published if entry > 15% of book | `long_horizon_cron.py:471-485` | Over-cap name floors qty to 0; no `shares==0` guard → 0-share card written. Latent (MRF ₹129,550 = 1 share); triggers on split-up or shrunk book. paper_broker correctly skips | Add to the entry-loop skip: `if _size_position(...)['shares'] < 1: continue` | high | no |
| W-19 | **LOW** | 🟠 amber | yfinance sole source, no 2nd-source reconciliation | `ohlcv_incremental.py:25,64` | Only OHLCV ingest is yf.download; split-heal compares yfinance against itself; would AMPLIFY a vendor mis-adjustment. Kite is dashboard-only | Cross-check yfinance close vs Kite /quote LTP for the ~15 selected names before publishing; degrade on >1% disagreement (cheap, owner already pays for Kite) | high | no |
| W-20 | **LOW** | 🟠 amber | Stale Render docs (CLAUDE.md/STRATEGY.md) | `cron-scanner.yml:2`; CLAUDE.md Deployment | render.yaml deleted; docs still describe the Render niftyquant-scanner cron. Doc drift only | Update CLAUDE.md Deployment table + STRATEGY.md to point at cron-scanner.yml; note pyarrow is a required out-of-lock install | high | no |
| W-21 | **LOW** | 🟢 green | Headline number ambiguity 30.26 vs 34.67 | `cpcv_long_horizon_final_682.json` vs `_tradelog_682.json`; `run_long_horizon_tradelog.py:62` | Frozen-cfg arm = 30.26%/1.15 (matches config.json, what live trades); the only per-trade file re-derives params → 34.67%/1.248. Both documented (not a bug) but baseline_v0 from the per-trade file anchors on the wrong variant | Regenerate the per-trade tradelog with `load_frozen_cfg()` so the artifact matches the live headline, or rename/flag it as the re-derived variant | high | partial (avoid mis-anchoring baseline_v0) |

## Safest fix order

1. **W-08** (one-line `.gitignore` allowlist) and **W-03** (one secret + env line) — trivial, high-leverage, no engine risk. Do first.
2. **W-06** (one line: append kill_log.jsonl to paper_files) + **W-13** (anchor fund-store path) + **W-18** (0-share guard) + **W-14** (log dropped names) — small, isolated, no behavior change to signals.
3. **W-01** (demerger CA-type adjustment) — the real correctness fix; ship behind a golden-master regeneration + a VEDL regression test.
4. **W-02** (universe union) + **W-04/W-10** (financials/coverage observability) — universe-scope changes; gate on the cloud expanded-universe re-derivation so the new universe is measured, not assumed.
5. **W-05 + W-11 + W-12** (booking-layer parity) — fix together so live paper matches the validated backtest exactly; add the open-key + aging regressions to the golden master.
6. **W-07 + W-09 + W-21** — reproducibility hardening (persist equity curve, hash pyarrow, fix per-trade headline) before standing up any Phase-2 research layer.
7. **W-15, W-16, W-17, W-19, W-20** — observability/ops/doc polish; lowest urgency.