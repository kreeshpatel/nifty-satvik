# Phase-1 Hardening — Fix Record (2026-06-27)

Closes the SAFE subset of the Phase-1 wiring-audit ledger (long_horizon/audit/wiring_issues.md).
All changes are live-path / observability / parity / tooling only -- the frozen cfg, feature math,
cleaner, backtest (portfolio.simulate) and decide_exit are UNTOUCHED. Proof: the golden master
(tests/test_long_horizon_golden.py) stays BYTE-IDENTICAL (re-run independently, 3 passed).

## Independent verification by the lead (not just the workflow verifier)
- `git status`: exactly 11 files modified + 3 new test files (the planned disjoint set).
- Golden master: **3 passed, byte-identical** (re-run twice).
- New regression tests: **34 passed** (demerger guard / exit parity / 0-share guard).
- Full repo suite (workflow verifier): **1814 passed, 8 skipped, 0 failed** (skips are pre-existing env/Docker skips).
- W-01 re-probe: `_demerger_suspect_names(ds.ohlcv-style frames)` flags **{VEDL, SKFINDIA}**; RELIANCE=False; entry loop excludes them (long_horizon_cron.py:572-574).
- W-05 diff: gap-down stop now books `decision.exit_price` (not the stop); `exit_days_held=max(0,days_since-1)`; human-facing `days_since` preserved.
- W-03: workflow env var `DRIFT_ALERT_SLACK_WEBHOOK` matches alert_publisher.py:147 exactly.

## Changelog (per W-id)

## Phase-1 Hardening — closed ledger items (verified)

| W-id | sev | file:line | what changed | guarding test | status |
|---|---|---|---|---|---|
| W-01 | HIGH | `src/runners/long_horizon_cron.py:106-162` (`_demerger_suspect` / `_demerger_suspect_names`), `:357-367` (compute suspect set in `_build_ranked_universe`), `:564-573` (entry-loop quarantine skip), `:73-74` (consts DEMERGER_LOOKBACK_BARS=263 / DEMERGER_DROP_THRESH=0.50) | Detect names whose RAW close has a >=50% non-reverting single-session drop within 263 bars (a value-leaving demerger that clean_ohlcv_for_features would back-adjust as a split and fabricate a soaring slope). Quarantine them from NEW entries only (held positions out of scope). Pure observability on raw frames — feature math / cleaner / backtest untouched. | `tests/test_lh_demerger_guard.py` (17 tests; VEDL→True real-cache, RELIANCE→False, threshold/recovery/lookback-window edge cases) | CLOSED — re-probed live: VEDL flagged (−64.9% session), in suspect set, skipped at entry loop |
| W-02 / AUD-007 | HIGH | `src/runners/long_horizon_cron.py:347-356` (diag membership_current_count / membership_only_in_universe / membership_only_current + `AUD-007:` print), `:716-717` (folded into cron_health membership_divergence/status) | Made the 48-invisible-current-members gap OBSERVABLE every run (was silent). Behavior UNCHANGED — the universe-union fix is deferred to Task-8 (new entrants lack PIT fundamentals). | (observability; covered indirectly by import smoke + re-probe) | CLOSED-as-documented — re-confirmed exactly 48, divergence < alert(75) so still a silent degrade, now printed |
| W-03 | HIGH | `.github/workflows/cron-scanner.yml:53` | Added `DRIFT_ALERT_SLACK_WEBHOOK` env (optional; owner must set the secret) so existing `_alert()` calls activate. | n/a (CI yaml) | CLOSED (gated on owner configuring the secret) |
| W-05 | HIGH | `src/trading/signal_tracker.py:478` (pass `open: current_open` into decide_exit's today_ohlc), `:521-536` (book `sig['close_price']=decision.exit_price` and recompute pnl from it, not from the stop level) | Live now records the REAL gap-fill on a stop exit (min(open,stop) when open<stop) instead of optimistically AT the stop — matches the backtest's gap-through fill. On a normal close-below-stop, exit_price==close (≤stop), preserving prior behavior. current_open defined at :375 (defaults to close when open absent → backward-safe). | `tests/test_lh_live_exit_parity.py::TestW05GapDownStopFill` (4) + existing `tests/test_exit_parity.py::test_decide_exit_stop_gap_fills_at_worse_of_open_close` / `test_decide_exit_stop_no_gap_fills_at_close` | CLOSED — no pre-existing test encoded the old optimistic fill, so none needed updating |
| W-06 | MED | `src/runners/long_horizon_cron.py` paper_files list (`results/kill_log.jsonl` appended next to kill_state.json) | Kill-verdict audit trail now pushed to GitHub so the observe-period verdict history survives ephemeral cron runs. push_results skips non-existent files → safe. | n/a (push-list; observability) | CLOSED |
| W-10 | MED | `src/runners/long_horizon_cron.py:430-450` (`_n_eligible_names`, `_n_no_coverage`, coverage print) | Distinguish 'absent from fund store (no coverage)' from 'present but D/E≥1.5 fail'; surface fundamentals coverage of the eligible large+mid universe. Filter UNCHANGED — observability only. | (observability) | CLOSED |
| W-13 | MED | `long_horizon/research/build_measurement_panel.py` (fund-store path anchored via `Path(__file__).resolve().parents[...]`) | Removed cwd-relative `data/fundamentals_pit_screener.pkl` so a non-root invocation no longer silently yields an empty universe. | (research tooling; AST + import clean) | CLOSED |
| W-14 | MED | `src/data/data_store.py:252-276` (collect `failed: list[(ticker,repr(e))]`, replace bare `except: pass`, print `[W-14] feature compute failed for N stock(s)` summary) | A crash in any (mostly unused) column no longer silently discards a whole name with no trace. Control flow UNCHANGED (name still skipped) — pure observability. | (observability; golden master proves no feature drift) | CLOSED |
| W-18 | LOW | `src/runners/long_horizon_cron.py:585-589` (`if int(sig.get('shares',0) or 0) < 1: continue`) | A name priced >15% of the ₹10L book floors to 0 shares at the position cap; no longer publishes a non-actionable 0-share BUY card — slot stays free for the next ranked name. | `tests/test_lh_zero_share_guard.py` (10 tests; cap-edge, base_risk_qty, ADV-cap, math identity) | CLOSED |
| W-08 | MED | `.gitignore:43` (`!results/ohlcv_cache_lh.json`) | Whitelisted the LH OHLCV cache so a `git clean`/`rm --cached` can't trigger a 600d cold-start (495-name yfinance hammer). | n/a (gitignore) | CLOSED |
| W-09 | MED | `.github/workflows/cron-scanner.yml:42` (`pip install pyarrow==20.0.0`) | Pinned the out-of-lock pyarrow install (pandas-3 compatible) so a PyPI blip doesn't empty the universe; TODO note to fold into requirements.lock. | n/a (CI yaml) | CLOSED |
| W-11 | MED | `src/trading/signal_tracker.py:466-481` (`exit_days_held = max(0, days_since - 1)` passed into decide_exit; sig['days_since'] human-facing age unchanged) | Live ages exits from the t+1 fill day, not signal_date(t), so the 63d hard cap and 10d min-hold fire on the same session as the backtest (was one session early). | `tests/test_lh_live_exit_parity.py::TestW11AgingParity` (4: min-hold not prematurely satisfied, hard cap fires day-64-not-63, min-hold suppresses time-stop, hard stop always fires inside min-hold) | CLOSED |
| W-07 / W-21 | MED/LOW | `diagnostics/run_long_horizon_tradelog.py` (+104 lines: persist equity_curve, frozen-cfg headline alignment), `long_horizon/results/README.md` (rehydration command + headline-variant note) | Persist equity curve into the committed artifact; document the 30.26%-frozen vs 34.67%-re-derived headline ambiguity so baseline_v0 anchors on the live-traded frozen-cfg variant. | (repro tooling/doc) | CLOSED |
| W-20 | LOW | `CLAUDE.md`, `long_horizon/STRATEGY.md`, `long_horizon/STRATEGY_FULL.md` | Updated stale Render docs to point at cron-scanner.yml; noted pyarrow as a required out-of-lock install. | n/a (docs) | CLOSED |

Notes: W-15 (github_push partial-success alerting), W-16 (Kite-refresh cron), W-17 (sub-50% demerger monitoring), W-19 (2nd-source reconciliation) were NOT in this changeset (no github_push.py / new-yaml diffs) — they are LOW-severity, out of the assigned scope, and remain open. The implemented W-ids match the present diffs exactly. All edited importable files AST-parse clean (the only `ast.parse` error was a shell cp1252 codec choking on the ₹ glyph in the zero-share test — re-checked under encoding='utf-8' → ok; pytest had already collected & run all 10 of its tests).

## Residual / deferred (NOT done here -- gated on the cloud backtest or data work)
- **W-02** universe union (48 invisible current members): a DATA no-op today (0/48 have fundamentals), so it ships with the Task-8 data work + the cloud re-derivation, not here. Divergence is recorded in cron_health.
- **W-01 full fix**: the live quarantine guard ships now; the *root* fix (CA-type-aware adjustment in clean_ohlcv_for_features) changes feature values -> gated on a golden-master regeneration + cloud re-derive.
- **W-04** financials D/E policy, **W-12** kill-equity entry anchoring, **W-15/W-16/W-17/W-19** ops polish: documented, lower priority.
- The headline backtest still must be re-derived on the cloud expanded universe before any number is trusted (W-07 makes the equity curve a persisted artifact so the baseline is reproducible).

_AUD-007 status: RE-CONFIRMED 48, UNCHANGED behavior, now documented. Probed live via load_membership()+current_members(today) vs config.NIFTY_500: current=500, config=500, (current - config) = 48 invisible current members (sample: ATHERENERG, GROWW-class new entrants — ABDL/ACUTAAS/AEGISVOPAK/ANTHEM/ATHERENERG/BELRISE/...), and (config - current) = 48 names that exited the index. Divergence 48 < MEMBERSHIP_DIVERGENCE_ALERT (75) so it stays a silent degrade — but is now surfaced every run via the `AUD-007: 48 current index member(s) absent from config.NIFTY_500 (invisible to the live scan)` print at long_horizon_cron.py:354 and the diag fields membership_only_current / membership_only_in_universe folded into cron_health. The universe-union FIX (adding new entrants to ds.stocks) is deliberately NOT applied — those names lack PIT fundamentals coverage, so it is sequenced to the Task-8 data work per STRATEGY_FULL.md._
_Regressions: none_
